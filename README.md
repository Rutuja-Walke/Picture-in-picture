# Alpha Screen
Picture in Picture allows users to watch videos in a floating
window (always on top of other tabs) so a user can keep
watching while interacting with other applications. Features: user can resize the window as well as move 
